#### crowsnest.conf
#### This is the default config after installation.
#### It is also used as the default config in MainsailOS.
#### For details on how to configure this to your needs, see:
#### https://github.com/mainsail-crew/crowsnest/blob/v5/README.md


#####################################################################
####                                                            #####
####      Information about ports and according URL's           #####
####                                                            #####
#####################################################################
####                                                            #####
####    Port 8080 equals /webcam/?action=[stream/snapshot]      #####
####    Port 8081 equals /webcam2/?action=[stream/snapshot]     #####
####    Port 8082 equals /webcam3/?action=[stream/snapshot]     #####
####    Port 8083 equals /webcam4/?action=[stream/snapshot]     #####
####                                                            #####
####    Note: These ports are default for most Mainsail         #####
####    installations. Using any other port would involve       #####
####    changing the proxy configuration or using URLs          #####
####    with the specific port like                             #####
####    http://<ip>:<port>/?action=[stream/snapshot]            #####
####                                                            #####
#####################################################################


[crowsnest]
log_level: quiet                      # Valid Options are quiet/verbose/debug
rollover_on_start: true                # Creates a backup and clears the log on every restart, if set to true
no_proxy: false                         # If set to true, no reverse proxy is required. Only change this, if you know what you are doing.

[cam 1]
mode: ustreamer                         # https://docs.mainsail.xyz/crowsnest/faq/backends
port: 8080                              # HTTP/MJPG stream/snapshot port
device: /dev/v4l/by-id/usb-Generic_USB_Cam_200901010001-video-index0
resolution: 1280x800                    # 2592x1944 2560x1440 1920x1080 1280x800 1280x720 1024x768 960x540 848x480 640x480
max_fps: 30                             # If hardware supports it, it will be forced, otherwise ignored/coerced.
#custom_flags:                          # You can run the stream services with custom flags.
v4l2ctl: focus_automatic_continuous=0,focus_absolute=255  # lock focus at current value

[cam 2]
mode: ustreamer                         # https://docs.mainsail.xyz/crowsnest/faq/backends
port: 8081                              # HTTP/MJPG stream/snapshot port
device: /dev/v4l/by-id/usb-DHZJ-220822-J_DICOTA_4K_01.00.00-video-index0
resolution: 1600x1200                    # 3840x3104 3840x2160 3264x2448 2592x1944 2048x1536 1920x1080 1600x1200 1280x720 640x480
max_fps: 30                             # If hardware supports it, it will be forced, otherwise ignored/coerced.
#custom_flags:                          # You can run the stream services with custom flags.
v4l2ctl: focus_automatic_continuous=1
